services:
  studio:
    # Keep devcontainer settings in one place.
    env_file:
      - ../.env
    volumes:
      # Mount the project version folder for sync. Uses BSM_VERSION from .env so it's
      # easy to switch versions in development without editing this file.
      - ../volume_versiones/${BSM_VERSION}:${BSM_DIR}

